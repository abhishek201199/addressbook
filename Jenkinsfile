pipeline {
    agent any

    stages {
        stage('compile') {
            steps {
                echo 'compile the job'
            }
        }
         stage('test') {
            steps {
                echo 'unit test'
            }
        }
         stage('package') {
            steps {
                echo 'this is pacakge'
            }
        }
    
        
        
        
        
    }
    
}
